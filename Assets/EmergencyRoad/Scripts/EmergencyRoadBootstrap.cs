namespace EmergencyRoad
{
    /// <summary>
    /// Runtime auto-composition was intentionally removed.
    /// Menu/Game controllers, views, audio, cameras and spawn prefabs must be authored in the scene
    /// and assigned through Inspector references.
    /// </summary>
    public static class EmergencyRoadBootstrap
    {
    }
}
